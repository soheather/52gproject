// 이 파일은 Supabase 연동 관련 파일이므로 내용을 비웁니다.
// 다른 파일에서 이 모듈을 import하는 경우가 있을 수 있으므로 완전히 삭제하지 않고 빈 객체를 내보냅니다.

export const supabase = {
  // 빈 객체로 대체
}

// 테스트 함수도 빈 함수로 대체
export async function testSupabaseConnection() {
  console.warn("Supabase 연동이 비활성화되었습니다.")
  return {
    success: false,
    message: "Supabase 연동이 비활성화되었습니다.",
  }
}

// 기타 함수들도 빈 함수로 대체
export const createProductsTableSQL = ""
